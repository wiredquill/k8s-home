apiVersion: apps/v1
kind: Deployment
metadata:
  name: plex
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plex
  template:
    metadata:
      labels:
        app: plex
    spec:
      containers:
      - name: plex
        image: rmt.example.com:5000/plexinc/pms-docker
        ports:
          - containerPort: 32400
        env:
        - name: TZ
          value: "America/Los_Angeles"
        - name: PLEX_CLAIM
          value: "claim-szM_ogtgsnx4nsi2VckA"
        - name: PLEX_UID
          value: "1000"
        - name: PLEX_GID
          value: "100"
        volumeMounts:
          # name must match the volume name below
          - name: nfs-rmt-plex-storage
            mountPath: /config
      volumes:
      - name: nfs-rmt-plex-config
        persistentVolumeClaim:
          claimName: nfs-detritus-suse-claim
